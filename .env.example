# --- Backend Spotify OAuth server ---
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
# For local dev the auth server runs on 5174
SPOTIFY_REDIRECT_URI=http://127.0.0.1:5174/api/spotify/callback
FRONTEND_URI=http://127.0.0.1:5173/
SPOTIFY_SCOPES=streaming user-read-email user-read-private user-read-playback-state user-modify-playback-state
ALLOWED_ORIGIN=http://127.0.0.1:5173
PORT=5174

# --- Frontend-only implicit auth (no backend) ---
# Uncomment these if you choose to skip the Node auth server and
# authenticate directly in the browser.
# VITE_SPOTIFY_CLIENT_ID=
# VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173

# --- Frontend API base override ---
# Only needed when the backend lives on a different origin/port than the UI.
# VITE_API_BASE_URL=http://127.0.0.1:5174
